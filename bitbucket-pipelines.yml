image: python:2.7

pipelines:
  default:
    - step:
        script:
          - apt-get -qq update
          - DEBIAN_FRONTEND=noninteractive apt-get -qq install -y python-pip python-dev git
          - pip install tox
          - tox