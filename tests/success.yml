stages:
  main:
    image: docker/centos6:v1
    steps:
      - ShellCommand:
          command: "ls -l"
      - TriggerStages:
          stage_names:
            - compile
            - test
          waitForFinish: True
      - TriggerStages:
          stage_names:
            - finalize
          waitForFinish: True
  compile:
    steps:
      - ShellCommand:
          command: "sleep 2"
  test:
    steps:
      - ShellCommand:
          command: "sleep 3"
  finalize:
    steps:
      - ShellCommand:
          command: "sleep 5"