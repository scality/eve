branches:
  default:
    stage: pre-merge

stages:
  pre-merge:
    worker:
        type: openstack
        image: 8 GB General Purpose v1
        flavor: Ubuntu 14.04 LTS (Trusty Tahr) (PVHVM)
    steps:
      - ShellCommand:
          name: exit 0
          command: exit 0