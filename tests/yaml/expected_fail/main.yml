branches:
  default:
    stage: pre-merge

stages:
  pre-merge:
    image:
        type: docker
        path: eve/centos7-ctxt
    steps:
      - ShellCommand:
          name: exit 1
          command: exit 1