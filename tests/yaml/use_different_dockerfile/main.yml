branches:
  default:
    stage: pre-merge

stages:
  pre-merge:
    worker:
      type: docker
      path: eve/use-different-dockerfile
      dockerfile: foo/bar/Dockerfile.buz
    steps:
      - ShellCommand:
          name: print "OK"
          command: echo "OK"
          haltOnFailure: True
