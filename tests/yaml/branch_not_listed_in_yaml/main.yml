branches:
  not-existing-branch:
    stage: pre-merge

stages:
  pre-merge:
    worker:
        type: docker
        path: eve/ubuntu-trusty-ctxt
    steps:
      - Git:
          command: exit 1
          haltOnFailure: True
