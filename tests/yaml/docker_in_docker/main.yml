branches:
  default:
    stage: pre-merge

stages:
  pre-merge:
    worker:
        type: docker
        path: eve/ubuntu-trusty-with-docker-ctxt
    steps:
      - ShellCommand:
          name: docker ps
          command: docker ps