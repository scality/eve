branches:
  default:
    stage: pre-merge

stages:
  pre-merge:
    worker:
        type: docker
        path: eve/bad-ubuntu-trusty-ctxt
    steps:
      - ShellCommand:
          name: exit 0
          command: exit 0