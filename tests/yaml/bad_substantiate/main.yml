branches:
  default:
    stage: pre-merge

stages:
  pre-merge:
    worker:
        type: openstack
        path: eve/bad_openstack_worker
    steps:
      - ShellCommand:
          name: exit 0
          command: exit 0
