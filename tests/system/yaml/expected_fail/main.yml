branches:
  default:
    stage: pre-merge

stages:
  pre-merge:
    worker:
        type: docker
        path: eve/centos7-ctxt
    steps:
      - ShellCommand:
          name: exit 1
          command: exit 1
          haltOnFailure: True