# requirements for successful installation of doc deps
#
# to recreate frozen requirements:
#
#    $ pip3 install -U -r requirements.txt
#    $ tox -e freeze-requirements
#
-c constraints.txt

towncrier
